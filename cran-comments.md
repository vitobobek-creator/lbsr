## Phase 2 development baseline

This is an internal pre-release package baseline and has not yet been submitted
to CRAN. Runtime verification should include:

```r
devtools::document()
devtools::test()
devtools::check()
```

The private canonical acceptance fixture is activated by setting
`LBSR_CANONICAL_CODING_BOOK` to the master coding-book workbook path.

